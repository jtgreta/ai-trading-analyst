# Environment template for ai-trading-analyst
# Copy to `.env` and fill in the real values.
# `.env` is gitignored - never commit real credentials.

# Telegram bot credentials (bot token + numeric chat id)
TG_TOKEN=your_bot_token
TG_CHAT=your_chat_id